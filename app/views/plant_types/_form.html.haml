= form_for @plant_type do |f|
  -if @plant_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@plant_type.errors.count, "error")} prohibited this plant_type from being saved:"
      %ul
        - @plant_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
